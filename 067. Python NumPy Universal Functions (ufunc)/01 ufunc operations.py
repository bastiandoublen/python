import numpy as np

x = [1, 2, 3, 4]
y = [4, 5, 6, 7]

z = np.add(x, y)
print("Vector addition:", z)

print("Sum of array:", np.sum(z))
print("Cumulative sum:", np.cumsum(z))
print("GCD of 36 and 60:", np.gcd(36, 60))
